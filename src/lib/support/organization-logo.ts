/**
 * 지원제도 운영기관 로고.
 *
 * 로고는 정부상징을 쓰는 중앙부처(청·처 포함)에만 붙인다. 지자체와 공공기관은 기관명 글자로 둔다.
 *
 * 지자체를 뺀 이유:
 *   - 지자체 제도 8,000여 건 중 광역은 1,392건뿐이고 나머지는 시·군·구다. 광역만 로고를 달면
 *     "서울특별시"에는 로고가 있고 바로 아래 "서울특별시 종로구"에는 없어 목록이 들쭉날쭉해진다.
 *   - 접두어로 잡아 붙이면 종로구 사업을 서울시가 한 것처럼 기관을 잘못 밝히게 된다.
 *   - 시·도 CI 는 사용 승인 절차를 두는 곳이 많다.
 *
 * 파일은 public/ministry-logo/<기관명>.svg 하나만 둔다. 로고가 올라가는 자리가 모두 흰 바탕이라
 * 어두운 바탕용 흰색본은 두지 않는다 - 필요해지면 그때 본색에서 변환한다.
 * 새 부처를 붙이려면 파일을 넣고 아래 목록에 이름만 더하면 된다.
 */

/**
 * 로고 파일이 있는 기관. support_programs.organization 과 글자가 정확히 같아야 한다.
 * 옛 이름(여성가족부·환경부·특허청)이 아니라 지금 데이터에 들어오는 이름으로 적는다.
 * 뒤 숫자는 지금 담긴 제도 수.
 */
const MINISTRIES_WITH_LOGO = [
  "보건복지부", // 173
  "농림축산식품부", // 159
  "중소벤처기업부", // 114
  "고용노동부", // 101
  "해양수산부", // 69
  "성평등가족부", // 62
  "산림청", // 43
  "교육부", // 39
  "국가보훈부", // 39
  "기후에너지환경부", // 34
  "산업통상부", // 30
  "국토교통부", // 29
  "문화체육관광부", // 19
  "지식재산처", // 18
  "행정안전부", // 17
  "과학기술정보통신부", // 16
  "통일부", // 14
  "질병관리청", // 13
  "경찰청", // 9
  "법무부", // 9
  "식품의약품안전처", // 5
];

const LOGO_SET = new Set(MINISTRIES_WITH_LOGO);

/** 이 기관의 로고 경로. 없으면 undefined - 부르는 쪽에서 기관명 글자를 그대로 보여준다. */
export function resolveOrganizationLogo(organization: string): string | undefined {
  return LOGO_SET.has(organization) ? `/ministry-logo/${organization}.svg` : undefined;
}
